{% extends 'base.html.twig' %}

{% block main %}
    {% for category in categories %}
        {% if category.isPublished %}
            <a href="{{ path('category', {'id' : category.id }) }}" style="color: {{ category.color }};">{{ category.title }}</a>
            <br>
        {% else %}
            <p>Cette categorie n'est pas encore publiée</p>
        {% endif %}
    {% endfor %}
{% endblock %}